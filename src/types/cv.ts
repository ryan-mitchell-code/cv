export interface Experience {
  company: string;
  role: string;
  start: string;
  end: string;
  achievements: string[];
}

export interface Profile {
  name: string;
  title: string;
  summary: string;
}
